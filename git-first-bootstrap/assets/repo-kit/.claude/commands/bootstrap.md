# /bootstrap

Use `bootstrap-agent`. Read `SETUP.md` and `AGENTS.md`, run `scripts/diagnose-env.ps1`, then guide prerequisite install, sync-from-repo, verify, and restart. Do not commit/push without user confirmation.
