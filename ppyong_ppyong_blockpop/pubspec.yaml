name: ppyong_ppyong_blockpop
description: "뿅뿅 블록팝 - 캐주얼 낙하형 블록 퍼즐. 엔진 독립 게임코어 (pure Dart). Flutter + Flame 앱의 lib/core 로 재사용."
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dev_dependencies:
  test: ^1.24.0
